## EduLink Nigeria — Full MVP Build Plan\n\nA clean academic university portal with **real email notifications today** + **mock SMS pipeline ready to swap in Termii later**.\n\n---\n\n### 1. Notification strategy (the key decision)\n\n**Email (real, today)** — Lovable's built-in email infrastructure\n- Set up an email sender domain (Lovable handles DNS)\n- Scaffold app emails with a branded `result-notification.tsx` React Email template\n- Send to every parent with an email on the `parent_links` row\n- Logged in `email_send_log` (Lovable's table) for audit\n\n**SMS (mocked, swappable)** — Built like real SMS, but no provider call\n- Single function `sendParentSms(parentLink, message)` lives in `src/lib/notifications/sms.ts`\n- Today: writes a row to `notification_log` with `status='mock_sent'` and logs to server console\n- Swap-in later: replace ONE function body with a Termii (or Twilio) `fetch` call — no other code changes\n- Admin \